<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xsi:schemaLocation="
        http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
        http://camel.apache.org/schema/spring http://camel.apache.org/schema/spring/camel-spring.xsd">

    <!-- setup ActiveMQ Artemis connection factory -->
    <!--<bean id="artemisConnectionFactory" class="org.apache.activemq.artemis.jms.client.ActiveMQJMSConnectionFactory">-->
        <!--<constructor-arg index="0" value="tcp://localhost:61616"/>-->
    <!--</bean>-->


    <!--<bean id="jms" class="org.apache.camel.component.jms.JmsComponent">-->
        <!--&lt;!&ndash; specify connection factory on JMS component &ndash;&gt;-->
        <!--<property name="connectionFactory" ref="artemisConnectionFactory"/>-->
    <!--</bean>-->

    <camelContext id="myCamel" xmlns="http://camel.apache.org/schema/spring">

        <route>
            <from uri="file://C:/camel/in"/>
            <!--<log message="Sending file ${file:name} to Artemis"/>-->
            <to uri="bean:xmlProcessor?method=process(${body})"/>
            <!--<to uri="jms:queue:data"/>-->
            <log message="Finish sending file to Artemis"/>
        </route>
        <!--<route>-->
        <!--<from uri="bean:fileReaderProcessor"/>-->
        <!--<to uri="bean:x"/>-->
        <!--</route>>-->

        <!-- turn on stream caching so we can stream big messages from Artemis
             to spool disk to avoid reading into memory -->
        <!--<route streamCache="true">-->
            <!--<from uri="jms:queue:data"/>-->
            <!--&lt;!&ndash;<log message="Received data from Artemis"/>&ndash;&gt;-->
            <!--<to uri="bean:XmlProcessor?method=process"/>-->
            <!--&lt;!&ndash;<log message="Finish saving data from Artemis as file"/>&ndash;&gt;-->
        <!--</route>-->

    </camelContext>

</beans>
